改动完成。改了 2 个文件：

**`server.py`** — `_SCHEMA_NOTE` 和 `memory_store_wrapper` docstring 加了合并优先指引：
- 存储前先 recall 同 domain 已有记忆
- 已有记忆覆盖此主题 → 用 `memory_update` 追加，不新建
- 优先合并到已有广泛记忆，避免窄条目的碎片化

**`_storage.py`** — `memory_store` 加了 nearby hint：
- `NEARBY_THRESHOLD = 0.25`（低于 0.25 不提示）
- Jaccard 0.25~0.6 之间 → 成功存储后附 `hint: nearby memories [...]` 提示
- 不阻断存储，只提醒调用 Claude 考虑合并

验证：存储 `memory-mcp-merge-rules` 时触发了 nearby hint（3 个 0.34-0.37 的记忆，无关误报但无害）。
