分析完了。当前 `memory_store` 的合并判断只有 Jaccard 文本相似度（阈值 0.6），纯词法匹配，不会做语义合并。

**核心问题不在存储层，在调用层**：`proxy-code-review-bugfixes` 和 `model-proxy` 内容确实不同，Jaccard 低于阈值是正确的。真正的问题是 Claude 调用 `memory_store` 时没有检查 "已有记忆的覆盖范围声明"。

**当前流程**：
```
新内容 → Jaccard vs 已有内容 → ≥0.6 则拒绝，<0.6 则新建
```

缺失的一环是**语义归属检查**——`model-proxy` 声明覆盖"已修 bug"，新 bug 修复就应合并进去。

**优化方案**：

| 方案 | 改动 | 效果 |
|------|------|------|
| **A. 工具描述指引** | 改 `memory_store` description，加 "存储前先 recall 已有记忆，检查覆盖范围声明，优先合并" | 零代码，靠 prompt 改善 Claude 判断 |
| **B. store 返回 merge hint** | Jaccard 0.25-0.6 时返回 "nearby: X (desc: ...)" 供调用方判断 | 低成本，词法层面提示 |
| **C. LLM 语义检查** | store 时调用 LLM 判断是否应合并到已有记忆 | 准确但增加延迟/成本 |

**建议先做 A + B**，C 等有更多误判案例再决定。

方案 A 直接改 `memory_store` 的 tool description 和 `_SCHEMA_NOTE` 就行。方案 B 在 `_storage.py` 里加几行逻辑。要动手吗？
