合并完成。删掉了 `proxy-code-review-bugfixes`，内容并入 `model-proxy`。

**判断逻辑的问题**：memory 自动生成时只看 scope 匹配（新记忆 scope=project vs `model-proxy` scope=infra），没检查内容语义归属。`model-proxy` 本身声明了"保留已修 bug"，proxy 相关的 bug 就该直接合进去，不需要再建独立记忆。

scope 用来分类是对的，但不能作为"不合并"的唯一依据——当内容明确属于某个已有记忆的覆盖范围时，应该合并而不是新建。
