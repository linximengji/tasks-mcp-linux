改完了。两个改动：

**config.yaml:75** — `deepseek-flash` → `deepseek-v4-flash`（匹配 proxy 路由表）

**memory_health.py** — 系统级失败熔断：
- 模块级 `_audit_cooldown` 计数器
- 如果单次 audit 全部 5 个候选都报错（系统性故障如模型名错误/proxy 挂了），设置 cooldown=3，跳过接下来 3 次周运行
- 任意候选成功则清零 cooldown
- daemon 无需重启，下次循环自动读新 config

这样不会再每周刷 5 条同样的 JSON 解析错误日志。
