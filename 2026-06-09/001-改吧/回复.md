改好了，两处变更：

**1. `pre_tool_guard.py`** — `dir` 加入 `_WINDOWS_INCOMPATIBLE`，下次在 Bash 里敲 `dir` 会拦截并提示用 `ls`。

**2. `post_failure_recorder.py`** — fallback 里过滤掉单字符和非字母开头的 binary 名，`=` 这类噪音不再记入 `binary_not_found`。
