## ops-daemon 运行记录总结 — 2026-06-09

### 时间线

| 时段 | 状态 | 关键事件 |
|------|------|---------|
| 01:32-01:34 | claudetalk 抖动 | claudetalk 反复停恢复 ×3，每次 2s 修复 |
| 01:34-07:15 | daemon 失联 | 心跳超时 18417s（~5h），直到 07:15 被 death_note 清理 |
| 07:15-07:20 | 晨间重启失败 | 启动后 4 分钟 pid_dead |
| 07:20-15:14 | 稳定运行 ~8h | 仅 08:00 触发一次 ssl_warn（天数从 26→25） |
| 15:14-15:41 | 崩溃风暴 | 27 分钟内连续崩溃 ×4，cloudflared 全线异常 |

### 突出问题

**1. cloudflared 隧道不通（15:14 起持续）**
- 连续 ~20 条 `cloudflared_no_connection`，涉及 8 个不同 PID
- 15:31 曾尝试 kill 重启，恢复 2 秒后又断
- 当前 pid 4248 处于 `degraded` 状态（进程活着但无 edge 连接）

**2. daemon 反复崩溃（15:14-15:41）**
- 4 次 death_note: pid_dead ×3, heartbeat_stale ×1
- 每次存活 1-10 分钟不等
- daemon 崩溃不影响 proxy (4000/4001) 和 claudetalk（PID 22196 始终在线）

**3. Memory 健康问题（每次启动都报，未见改善）**
- 7 条 broken link（deepseek-sanitize → model-proxy, ops-daemon → links 等）
- 5 个文件 JSON 解析失败（bailian-token-plan-usage, deepseek-cdp-automation 等）
- 3 对重复记忆提示

**4. 6 个 MCP 服务均为僵尸**
- openscad/drawio/image-gen/pdf/p1s/blender 进程始终独立运行，不被 daemon 管理
- 僵尸 PID 跨 daemon 重启不变（2440, 3344, 6060, 11500, 12972, 23052）

### 运行良好的

| 组件 | 状态 |
|------|------|
| Proxy 4000/4001 | 始终 up，延迟 0.3-15.7ms |
| claudetalk | PID 22196 全天在线 |
| 系统资源 | CPU 1-13%，内存 ~32%，D 盘 0.8% |
| C 盘 | 67.6%，64.8GB 余量 |

### 建议

cloudflared 隧道不通是下午的根因，可能触发了 daemon 的某种崩溃连锁。如果 term.linximengji.com 从外网访问不了，先查 cloudflared token 或网络环境。另外 5 个 JSON 解析失败的记忆文件可以考虑清理或修复。
