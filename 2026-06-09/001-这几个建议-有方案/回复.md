Mining 是 CC 内置功能，不是我的脚本，所以改不了逻辑。但可以这样处理：

**方案 A：不动（建议）**
- `skip_llm: true` — 省 token 的，放着就行。知识精炼需要时手动跑一次 `cat ~/.claude/mined/*_knowledge.json | jq` 看一眼足够
- 重复 run — 偶尔一次，不影响结果，不管
- duration 为 null — 大部分是 `session_id|turn_count` 格式的 session，CC 内部没有记录结束时间，没救

**方案 B：追加一个后处理脚本**
如果你确实想要语义洞察，可以写个独立的 Python 脚本：
1. 按周聚合 `*_memory.json` → 按主题聚类出 top 话题和耗时趋势
2. 跑一次 LLM 调用生成周报摘要
3. 飞书/钉钉推给你

大概 ~80 行，挂个 cron 每周日跑一次。要不要？

**方案 C：留到以后做**
等 Claude Code 开放 mining pipeline 的配置文件时再调，现在硬改 `latest_run.json` 会被覆盖，没意义。

倾向于 B 的话，我出个方案设计确认再写。
