就是删掉 L1 的 rule 2：

```python
# Rule 2: recent tool use → pro
if _has_recent_tools(messages):
    return "deepseek-v4-pro", "L1:recent-tools"  # ← 删掉这 3 行
```

删了之后，所有非图片、非 trivial、非超长消息都走 L2 分类器，由 Flash 判定复杂度。

代价：每条消息多 200ms + 50 tokens。效果：300+ 条原本被截胡的请求有机会分到 flash 或 qwen3.7-max。
