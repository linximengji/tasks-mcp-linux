结论很清楚：

**根因：`proxy_backup.py` 文件不存在。**

watchdog 的 backup lifecycle（`watchdog.py:178-184`）在循环重试：
1. backup_proc 退出（`pythonw` 找不到文件，rc=1）
2. 等 2 秒 → 重新 `start_backup()` → 又失败 → 循环

日志 `proxy_backup.log` 共 6763 行，边界在 line 322→323：
- **1-322 行**：`DeprecationWarning`（on_event deprecated）→ 文件曾存在且运行过
- **323-6763 行**：全部是 `can't open file: [Errno 2] No such file or directory`，无限循环写入

文件无备份（`backups/` 下没有，`.claude` 没有 git），无法从历史恢复。内存文档里写的 78 行文件，在某个时刻被删除了——可能是某次 refactor 期间误删，具体时间点无法精确确定。

**当前影响**：
- 主代理 4000 ✅ 正常
- 备份代理 4001 ❌ 永远起不来
- 如果 4000 挂掉，watchdog auto-switch 切到 4001 会彻底断连，fallback 形同虚设

需要我重写 `proxy_backup.py` 吗？按内存文档描述：78 行，仅 DeepSeek 直通，独立不依赖 proxy_lib。
