完成。改动总结：

**删除的内容**：
- `PRICING` 字典（23-73 行，8 模型 × 3 档定价）
- `_MODEL_ALIASES` 映射表
- `_normalize_model()` 函数
- `calc_cost()` 函数
- `_fmt_cost()` 函数
- 日报/周报/月报 HTML 模板中所有"费用"列
- Excel 报表中"费用(CNY)"列和 Provider 分配 sheet
- 命令行输出中费用统计

**保留的内容**：
- Token 数量统计（输入/输出/调用次数）
- Provider 余额查询（balance_query 每日余额变化 = 实际消费）
- 充值记录（recharge_records.jsonl）

原因：之前 `calc_cost` 用硬编码单价 × token 数估算费用，准确度受限于定价更新不及时（如 cache_hit 定价错误一直没修正）。现在费用端由 `balance_query` 直接追踪各 provider 余额变动，token_stats 只负责 token 数量统计，两者分工清晰。
