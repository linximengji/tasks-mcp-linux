生产代理运行后，路由日志干干净净——只有 `trivial` 和 `very-long`，没有 `recent-tools`。

**旁路测试 + 上线完成**。变更总结：

| 文件 | 改动 |
|---|---|
| `router.py:183-185` | 删除 `_has_recent_tools` 规则（Rule 2） |
| `model_proxy.py:84` | `max_tokens: 20` → `100`，关闭 thinking |
| `handlers.py:125` | `if out:` → `if inp or out:` |

**路由拓扑变为**：

```
请求
  ├─ L1: 有图片 → qwen3.6-plus
  ├─ L1: 短问候 → flash
  ├─ L1: 超长(>8K) → qwen3.7-max
  └─ 其余 → L2 Flash 分类器
           ├─ trivial/simple → flash
           ├─ moderate → pro
           └─ complex → qwen3.7-max
```

之前 90% 被 `recent-tools` 截胡的请求现在会走 L2 合理分配了。
