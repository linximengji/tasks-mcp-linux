充值链路完成。整体改动：

**`balance_query.py`** 新增：
- `recharge_records.jsonl` — 手动充值记录文件，格式 `{date, provider, amount, currency, ts}`
- `record_recharge()` — 写入函数
- `_recharges_between()` — 区间查询，`(start, end]` 半开区间防止重复计数
- CLI: `python balance_query.py recharge <provider> <amount> [date]`
- `_daily_spend_for()` 同时整合自动检测（DeepSeek topped_up/granted）和手动记录

**使用方式**：充完值跑一条命令即可，次日日报自动校正。
```
python balance_query.py recharge DeepSeek 100
python balance_query.py recharge "DashScope Token Plan" 200 2026-06-02
```

之前有个 pending 项还不确定要不要修 —— cache_hit 定价（现在 ¥0.025/M，实际 ¥0.50）和 proxy 丢失 workDir/session_id 字段。需要我处理吗？
