备份代理已恢复。E2E 验证：

| 端点 | 状态 |
|---|---|
| `/health` | ✅ `{"status":"ok"}` |
| `/v1/models` | ✅ 返回 deepseek-v4-pro/flash |
| `/v1/messages` non-stream | ✅ "1+1=2" |
| `/v1/messages` stream | ✅ SSE 透传正常 |

`proxy_backup.log` 已停止增长（不再有 `can't open file` 循环），watchdog 已自动接管新进程。三层故障转移现在完整了：

```
4000 主代理 (full routing) ✅
  ↓ watchdog auto-switch (3 次连续失败)
4001 备份代理 (DeepSeek 直通) ✅   ← 刚修复
  ↓ 手动切 settings.json.direct
MAAS 直连 (Token Plan)
```
