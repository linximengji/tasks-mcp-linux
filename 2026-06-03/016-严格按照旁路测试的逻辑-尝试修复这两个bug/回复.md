两个 bug 已修复，生产代理验证通过：

```
L2: simple + low -> deepseek-v4-flash    ← 原来是 moderate + ?
```

**修复内容**：

| Bug | 位置 | 改动 |
|---|---|---|
| thinking 消耗 20 tokens，budget_est 永远来不及输出 | `model_proxy.py:84` | `max_tokens: 100`, `thinking: disabled` |
| stream guard `if out:` 在 qwen output=0 时丢弃所有 token | `handlers.py:125` | `if out:` → `if inp or out:` |
