两个问题：

1. **drawio MCP 超时** — `new_diagram` 和 `export_diagram` 两个 MCP 工具都 30s 超时，drawio CLI 启动慢，直接调用 MCP 工具靠不住。

2. **路径不匹配** — 我把 `.drawio` 写到了 `D:\ClaudeProjects\proxy-routing-flow.drawio`，但 drawio MCP 的 project 目录是 `D:\ClaudeProjects\drawio-mcp\diagrams\`，所以 `export` 找不到文件，报了 `FileNotFoundError`。`cp` 过去就好了。

3. **CLI 参数格式错误** — MCP 工具内部拼的命令是 `export --format png --scale 2`，但实际 CLI 的语法是 `export render <output> -f png --scale 2`，参数结构完全不同。绕过 MCP 直接调 CLI 就对了。

根因还是 MCP 工具封装和实际 CLI 接口没对齐。
