备份代理 **4001 端口挂了**。当前状态：

| 层 | 端口 | 状态 |
|---|---|---|
| **主代理** (full routing) | 4000 | ✅ UP |
| **备份代理** (DeepSeek 直通) | 4001 | ❌ DOWN |

当前 Claude Code 走的是 4000 主代理，工作正常。但第二层防护（4001 备份）不在线——万一 4000 挂了，watchdog 的 auto-switch 会切到 4001，但那会直接不可用。

要排查一下为什么 4001 没起来吗？可能原因：watchdog 启动 4001 失败、端口被占用、或者 `proxy_backup.py` 有问题。
