不需要分割。

你的行为模式跨项目一致。看数据：

- 130/147 session 都在 `D--ClaudeProjects`，其他 workspace 各只有个位数
- Top tools（Bash/Read/Edit/PowerShell/Write）跨项目完全相同
- `cli` 和 `sdk-cli` 两入口用同样的工具链

**分割的问题**在于稀释样本。claudetalk 7 sessions 产生的轮次太少，没法独立挖出有统计意义的模式。1907 轮次合在一起才有价值。

**需要的是过滤能力，不是分割存储**。加个 `project` 标签在 session 表上即可——挖模式时默认聚合，偶尔要看"claudetalk 项目的 tool 偏好"时按 project 过滤就行。

要我把 project 标签加上去吗？
