计划写在 `.plan.md` 了。核心变更：

**改前：** `fetchAll` → `translateBatch` (API) → `humanizeBatch` (API, 可选) → `classifyNews` (关键词)  
**改后：** `fetchAll` → `llmProcessBatch` (单次 API，过滤+分类+翻译)

要点：
- fetch.ts 关键词初筛保留去噪声
- 一次 API 调用同时做：废弃判断、7类分类、翻译、润色
- `max_tokens` 提到 16384（80 条×摘要撑得住）
- 加 `--skip-llm` 回退关键词模式（API 故障时备用）

5 个 task，改 3 个文件新建 1 个文件。确认开干？
