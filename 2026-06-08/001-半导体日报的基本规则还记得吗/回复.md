记得。核心流程：

**筛选** — 每个 RSS 源配有关键词列表，title 命中 1 个或 summary 命中 ≥2 个就收录（再加一个硬排除列表 `EXCLUDE_TITLE_KEYWORDS` 去噪）。跨天去重靠首 50 字符，回溯 3 天。

**分类** — `text.includes(kw)` 逐类别匹配，先到先得，落选归"其他"。7 个类别：设备材料、制造工艺、先进封装、芯片产品、EDA/IC设计、市场供应链、产业政策。

**翻译/润色** — 英文内容走 DeepSeek 翻译（`[N] 中文标题 — 中文摘要`格式），再用一次调用去 AI 味。可选，没配置 API key 就跳过。

**产出** — HTML 报告 + JSON 存 `reports/` 目录，163 SMTP 发送邮件。

**管线顺序**：fetch → translate → humanize → classify → render → save JSON → send email

---

回到之前的方案，你还没回复那 3 个确认点。先定下来再动手？
