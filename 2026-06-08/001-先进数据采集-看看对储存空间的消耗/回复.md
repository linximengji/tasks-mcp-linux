数据已采集，消耗清楚了。

**建议：日清理。**

理由：
- 耗时可忽略（0.7s 扫全量，增量更快）
- 日均写入不到 200 KB，无感
- 周清理延迟太长——你刚才的 session 要等 7 天才进索引，模式挖掘慢半拍
- 逐 session 收益不匹配——hook 触发比 cron 复杂，数据量小不值得实时

用 Windows Task Scheduler 或 cron 挂一个每天凌晨跑的 `collect_sessions.py` 即可。

下一步：在日清理基础上，定**模式识别**和**记忆提取**的策略？即从这 1907 轮交互里提取什么、怎么提取。
