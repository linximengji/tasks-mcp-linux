目前完全是**原始 JSON** dump 到 `~/.claude/mined/`，四个文件：

| 文件 | 大小 | 内容 | 评价 |
|------|------|------|------|
| `tool_stats` | 5.8KB | 工具频次、共现对、入口统计 | 数据扎实，可直接用 |
| `patterns` | 0.8KB | 错误分布、thinking 效果 | 弱——当前行没有 error 记录，全是 0 |
| `knowledge` | 4.6KB | 15 条启发式知识项 | 标签太粗（全是 `toolchain`），没有 LLM 提炼 |
| `memory` | 120KB | 147 条 session 摘要 | 纯量化指标 + 逐条 verbatim 记录，未压缩 |

问题是：

1. **没人消费** — 写进去就完了，没有查询接口，没有聚合报表
2. **knowledge/memory 没有 LLM 参与** — heuristic 标签区分度不够，memory 的 `user_ask_summary` 只是把原始输入抄进来
3. **增量数据逐日累积** — 但只看 latest_run 的话，每天产出几行数据（没有新 session 就近乎空文件）

你问"怎么输出"——是想决定**存什么格式**（JSON 还是别的）？还是想要**可视化/查询入口**（比如丢到飞书、生成周报、Web 页面）？还是想先解决目前数据质量的瓶颈再谈输出？
