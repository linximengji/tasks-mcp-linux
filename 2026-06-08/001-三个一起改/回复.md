这三处改动上次对话已经全部完成并生效：

1. **Patterns** — 5 个新维度：工具链序列、时长分布、工具丰富度、不同 entrypoint 的行为对比、首发工具统计
2. **Knowledge** — 8 类 heuristic 标签 + 手动运行时额外 LLM 提炼 top 20% 高价值 session
3. **Memory** — 低信号过滤（<3 turns 或 <2 tool_calls 跳过）+ 手动运行时 LLM 中文摘要

已经是当前代码状态，可以直接跑。试试 `cmd /c daily_pipeline.bat` 走日常增量，或 `python mine_sessions.py`（不带 `--skip-llm`）看 LLM 增强效果。
