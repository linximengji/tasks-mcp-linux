好的，压缩版本：

**CC 内置（~/.claude/）：**
- `history.jsonl` — 所有 session 的用户输入历史
- `projects/{slug}/{sessionId}/subagents/agent-{id}.jsonl` — 子 Agent 完整对话
- `file-history/{sessionId}/` — 文件版本快照
- `sessions/{pid}.json` — 进程级 session 元信息

**Hook 层（~/.claude/hooks/）：**
- `audit/tool-audit-{date}.jsonl` — Edit/Write/Bash 执行记录
- `failure_memory.json` — tool 执行失败分类归因

**claudetalk 项目：**
- `.claudetalk/claudetalk-{date}.log` — 操作日志（含 prompt）
- `.claudetalk/token_usage.jsonl` — token 用量和费用
- `.claudetalk-sessions.json` — 飞书/钉钉会话状态
- `.claudetalk/dingtalk/history_{id}.json` — 钉钉聊天历史
- `../phone-tasks/{date}/{seq}-{slug}/` — 飞书消息归档

需要看哪个的具体结构或格式？
