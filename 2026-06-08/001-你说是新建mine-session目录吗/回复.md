不是。我说的是 `digest_to_memory.py` 这个脚本做的事：

1. **读** daily mine 已有的产出文件（`mined_knowledge.json`）
2. **写**到共享 `memory/` 目录下的 `projects/<name>/` 子目录

不需要新建目录。daily mine 的输出文件路径不变，digest 只是从中提取优质知识条目，写入共享 memory。

让我看看 daily mine 当前的输出放哪的——`mined_knowledge.json` 路径是啥？
