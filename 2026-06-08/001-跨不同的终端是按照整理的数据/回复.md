数据按 **workspace**（Claude Code 启动时所在的 `cwd`）组织。

CC 在 `~/.claude/projects/` 下为每个工作目录创建一个子目录，命名规则是把路径中的 `:` 和 `\` 替换成 `-`。目前有 6 个 workspace：

| workspace | session 数 | 说明 |
|-----------|-----------|------|
| `D--ClaudeProjects` | 130 | 主力工作区 |
| `D--ClaudeProjects-claudetalk` | 7 | claudetalk 项目 |
| `D--ClaudeProjects-p1s-mcp` | 1 | p1s-mcp 项目 |
| `D--ClaudeProjects-ops-daemon` | 1 | ops-daemon |
| `C--Users-34971--claude` | 1 | 全局配置 workspace |
| `D--ClaudeProjects-test` | 1 | 测试 |

每个 .jsonl 文件是一次 CLI 调用的完整 session。DB 的 `sessions` 表已经有 `cwd` 和 `file_path`，但缺一个干净的 `project` 字段。要不要我加进去？这样后面按项目切分查询就方便了。
