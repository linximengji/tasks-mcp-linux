上一轮已经改完了，代码在 `main.py` 里（`repair.py` 是修复函数注册表，只有修复动作没有退避策略——退避逻辑在 `main.py:on_checks` 和 `_claudetalk_lifecycle` 中）。

变更确认：

```
ops-daemon/ops_daemon/main.py  三个改动点：

1. L254-256  — 阶梯退避状态变量
2. L306-322  — 通用 exhaust handler 支持阶梯超时
3. L374-421  — claudetalk 生命周期：阶梯退避 + 恢复通知
```

**效果**：15s 内崩 5 次 → 退避 5min → 崩 → 15min → 崩 → 1h，每次退避到期自动重试。恢复后飞书通知。
