改完了。三个改动的总结：

1. **`config.yaml`** — repair 段加了 `exhaust_timeout_seconds: 3600`（默认 1 小时）
2. **`main.py` repair loop** — 进 exhaustion 判断前先检查时间戳，超时后自动清空 repair state 重新尝试
3. **`main.py` rapid crash handler** — 设 `repair_exhaust_at` 时间戳，让上述超时机制也能覆盖快速崩溃锁定

效果：claudetalk 崩溃后 daemon 不再永久放弃，而是每 `exhaust_timeout` 秒重置、再试 `repair_max` 次，循环下去。默认 1 小时复位一次，可以改 `config.yaml` 里的值调短（比如 900 = 15分钟）。

重启 daemon 后生效。你可以观察一段时间，看看 claudetalk 是恢复了稳定、还是反复崩溃 -> 1小时后再试的模式。如果以后找到 root cause 了，再把 `exhaust_timeout` 关掉或调大就行。
