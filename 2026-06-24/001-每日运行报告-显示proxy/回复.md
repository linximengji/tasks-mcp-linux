已经调查过了。浓缩一下结论：

**根因链：**

1. **daemon.pid 文件写入了 UTF-8 BOM** — 6/23 凌晨 daemon 重启时读 `daemon.pid` 读到 `\xef\xbb\xbf64276`，`int()` 崩溃，daemon 直接退出
2. **daemon 挂了 → 主 proxy（4000）无人看护退出** — 进程不在，curl 返回 "目标计算机积极拒绝"
3. **check-daemon 每 10 分钟尝试重启 daemon → 读取相同的 BOM 文件 → 重复崩溃** — 形成死循环，几次后放弃尝试
4. **备份 proxy（4002）正常运行** — 一直在线，`/health` 返回 `{"status":"ok"}`
5. **auto-recovery 有缺陷** — episotic 日志显示 6/22 23:56 判定 "stable" 切回 main，但 main 实际不可达，立即又检测到 down，形成切换噪音

**当前状态：**
- 主 proxy (4000)：**下线**（进程不在了）
- 备份 proxy (4002)：**正常**
- ops-daemon：**下线**
- Jaeger OTel (4317)：**下线**（daemon 挂了所以 OTel 导出一直 UNAVAILABLE）

**怎么恢复（先记着，不处理）：**
1. 删 `data/daemon.pid`
2. 重启 ops-daemon → 它会恢复主 proxy 看护
3. 修复 auto-recovery 的 "stable" 判定条件——真正确认 4000 可达再切回
