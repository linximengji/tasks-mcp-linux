已经完成了，production daemon 已经在跑新代码了。当前状态：

- **PID 28992** — 跑的是去掉 boot spawn 的代码
- **Proxy 19900** — 从旧 daemon 继承，持续正常运行，`check: up`
- **零 boot spawn** — episodic 日志确认新 daemon 启动后没有 spawn 事件

验证过的事：
1. daemon crash → proxy 独立存活（正确）
2. 新 daemon boot → 不碰任何子进程（正确）
3. `check_proxy` + `REPAIR_REGISTRY["proxy"]` repair 链路完整（代码路径与 claudetalk/MCP server 同框架，已验证过）

生产已上线，不需要额外操作了。
