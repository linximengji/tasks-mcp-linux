目前**仅在以下情况**通知：

- **修复耗尽**（`repair_max` 次尝试全失败）→ 发一条 CRITICAL
- **快速崩溃超阈值**（15s 内 5 次）→ 发一条 CRITICAL
- **修复成功** → 发一条 INFO

但普通崩溃（每次检测到 `stopped`）**不通知**。6/24 风暴中那几十次 `claudetalk_stopped` 就只有 episodic 日志，没有飞书消息。尤其是 repair 已 exhausted 后，后续所有 crash 事件都被静默跳过。

要不要加一个：**每次检测到 claudetalk 停就发通知**？但那样 6/24 的情况会刷屏。或者加个**限频**（比如 5 分钟内最多 1 条）？
