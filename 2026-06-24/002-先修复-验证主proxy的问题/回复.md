这些我已经全部完成了。总结一下已做的事和结论：

## 已完成

**修复**：杀掉僵尸 daemon（PID 31440，因 BOM 崩溃后锁住 PID 文件），重启 daemon → 主 proxy 4000 恢复。

**验证结果**：

| 组件 | 状态 |
|---|---|
| 主 proxy (4000) | ✅ `{"status":"ok"}` |
| 备份 proxy (4002) | ✅ `{"status":"ok"}` |
| ops-daemon | ✅ latest.json 正常刷新 |

## Bug fix 审查结论

**昨天的修改没有引入问题。** 三个文件的改动都是安全的：

- `model_proxy.py`：budget 计算重构 + stuck escalation 新功能 + thinking 混合历史检测 — 纯新增/重构，逻辑正确
- `proxy_lib/sanitize.py`：递归清理 `tool_result` 中的 thinking 块 — 修复之前未覆盖的 case
- `proxy_lib/telemetry.py`：实时 credits 计算 + 60s 缓存 — 逻辑正确

**真正原因**：proxy 是 23:49 被外部 `/v1/restart` 请求主动杀掉的，不是代码 bug。之后 daemon 僵尸进程（BOM 崩溃循环）锁住了恢复链，导致 proxy 4000 无人恢复。

还有什么需要深入的吗？
