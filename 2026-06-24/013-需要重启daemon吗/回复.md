需要。两个原因：

1. **`config.yaml` 里的 `exhaust_timeout_seconds: 3600`** — daemon 只在启动时读一次配置，不改不生效
2. **repair state 已耗尽** — claudetalk 处于 exhausted 状态，重启 daemon 后状态清空，daemon 会重新尝试拉起 claudetalk（用的是你刚编译的新 `dist/cli.js`）

要现在重启吗？通过 `stop.ps1` 优雅停 → Task Scheduler 10分钟内自动拉，或者我直接触发 `.stop` 标记？
