已经实现并验证完成了。当前状态：

**已创建/修改的文件：**
- `~/.claude/hooks/post_failure_recorder.py` — 失败记录 hook
- `~/.claude/hooks/pre_tool_guard.py` — 前置拦截 hook
- `~/.claude/settings.json` — 已注册两个 hook

**验证通过的场景：**
- 记录 `binary_not_found`（apt-get 等）
- 记录 `module_not_found`（Python 模块）
- count<3 时 systemMessage 警告但不拦截
- count≥3 时 exit 1 硬拦截
- `&&` 在 PowerShell 5.1 下识别并拦截
- 未知命令放行

**记忆文件：** `~/.claude/failure_memory.json`，跨 session 跨项目共享。手动删条目即可恢复通行。

后续自然累积失败数据即可，你有需要手动预填的已知失败命令吗（比如 Windows 上肯定没有的 Linux 工具）？
