**待办任务：开通飞书 ASR 权限并测试语音输入**

**背景：** 已实现飞书语音输入功能（`recognizeSpeech()` 方法），调用飞书 `speech_to_text/v1/speech/recognize` API 将语音消息转文字后喂给 Claude。

**需要完成：**
1. 飞书开发者后台 → 应用 → 权限管理 → 添加「语音识别」权限（`speech:speech_to_text`，灰度开放）
2. 权限添加后，发版重启 claudetalk
3. 在飞书私聊/群聊发一条语音消息，确认返回识别后的文本回复
