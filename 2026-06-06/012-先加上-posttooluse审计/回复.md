好了。已添加：

**`~/.claude/hooks/audit_logger.py`** — PostToolUse hook，监听 `Edit|Write|Bash`，在 `~/.claude/audit/tool-audit-YYYY-MM-DD.jsonl` 逐条追加审计日志。

每条记录包含：时间戳、工具名、摘要（如 `Edit src/foo.ts (42→156 chars)`）、状态。当日文件轮转，不积累。
