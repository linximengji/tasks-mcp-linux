改完了。来确认改动范围：

**3 处改动，1 个新方法：**

1. **支持类型白名单**（line 789）— `audio` 加入 Set，语音消息不再被拒
2. **`parseFeishuMessage` 新增 audio 分支**（line 1023）— 解析 `file_key`，调用 `recognizeSpeech`，识别文本直接作为 `messageText` 流入下游 Claude 处理流程
3. **历史消息也加了 audio 分支**（line 1363）— 群聊历史回顾时同样走 ASR，拼成 `[语音: 识别文本]` 格式
4. **新方法 `recognizeSpeech()`** — 调用飞书 `speech_to_text/v1/speech/recognize`，参数 `file_type: 'opus'`（飞书语音消息的编码格式）

**需要注意的一件事：** 飞书开放平台需要在应用权限里申请 **`speech:speech_to_text`** 权限，目前是灰度开放——去飞书开发者后台 → 权限管理 →「语音识别」添加这个权限。如果没有这个权限，ASR 会返回错误码，消息会显示 `[语音消息识别失败]`。

可以直接启动测试。
