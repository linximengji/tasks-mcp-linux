**全部通过。** 类型检查零错误，`tsc` 编译成功，35/35 测试通过。

改动已就绪，生产可用：
- `classifier.ts` 三分类（`completed` / `task-pending` / `reference`）
- `phone-archive.ts` 自动标记已完成任务
- `dist/` 已编译，下次 claudetalk 启动即生效

重启 claudetalk 后，对话就会自动走三分类了。
