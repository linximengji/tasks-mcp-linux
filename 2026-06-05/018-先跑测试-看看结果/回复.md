全部通过。新加的 `thinking blocks stripped for DeepSeek` 测试确认带有 `thinking` 块的 assistant 消息经过 `sanitize_for_deepseek` 后不会报错。

结果汇总：
- 改动：从 `ALLOWED_BLOCK_TYPES` 移除了 `thinking`/`redacted_thinking`/`tool_reference`
- 测试：12/12 全 PASS，含新加的针对性测试
- `recent-tools -> pro` 的 FAIL 是路由测试的已知问题，与本改动无关

需要我执行 `proxy_restart.py` 来让生产 proxy 加载这个改动吗？
