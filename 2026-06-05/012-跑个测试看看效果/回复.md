全部用例输出符合预期：

| 步骤 | 输出 |
|---|---|
| Read | `📖 1. 读取 src/index.ts ✓` |
| Edit | `📝 2. 修改 src/core/step-summarizer.ts ✓` |
| Bash | `💻 3. npm test -- --coverage ✓` |
| Grep | `🔎 4. 搜索 editStatus ✓` |
| Agent | `🤖 5. Explore push logic ✓` |
| WebSearch | `🌐 6. 搜索: TypeScript decorators ✓` |
| Glob | `🔍 7. 搜索 src/**/*.ts ✓` |
| 未知工具 | `🔧 8. UnknownTool ✓` |

每个 step 只推一条，`tool_use` + `tool_result` 合并为 **一句已完成叙事**，不刷屏。

还要做别的验证，还是可以收工了？
