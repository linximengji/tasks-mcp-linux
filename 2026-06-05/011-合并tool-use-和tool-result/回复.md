编译通过，零错误。

---

### 改动总结

**新文件** `src/core/step-summarizer.ts` (~50 行)  
将工具名+入参 → 自然语言一句话总结。

**修改文件** `src/index.ts`  
流式回调中：

| 事件 | 之前 | 现在 |
|---|---|---|
| `thinking` | 每次推 🧠 **深度思考中...**（重复刷屏） | 只在第一段思考推一次 🧠 **思考中...**，后续思考跳过 |
| `tool_use` | 🔧 **调用工具: Read** | 存到 `pendingStep`，**不推送** |
| `tool_result` | 📋 **分析工具结果...** | 取 `pendingStep`，推合并总结 **📖 1. 读取 src/index.ts ✓** |
| `text` / `result` | 不变 | 不变 |

**最终效果（手机上看到的消息演变序列）：**
```
⏳ 处理中...                     ← 初始
🧠 思考中...                     ← 首次思考（仅一次）
📖 1. 读取 src/index.ts ✓       ← step 1
📝 2. 修改 src/index.ts ✓       ← step 2
💻 3. npm test ✓                ← step 3
最后回答内容...                   ← 最终推送完整回复
```

不再有连续 `深度思考中...` 刷屏，每步一句自然语言总结。可以用 git commit 了，要我执行还是你来做？
