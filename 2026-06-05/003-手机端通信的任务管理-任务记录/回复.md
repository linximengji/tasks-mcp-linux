梳理完了。现状很清晰：

**✅ 已正常的功能：**
- `archiveConversation` — 每次对话完成后自动归档，保存 `消息.md` / `回复.md`
- `classifier` — 分类器正常，调用 deepseek-v4-flash 判断 task（待办）还是 reference（归档）
- `index.json` — task 类写入 `task-pending` 状态
- 现有数据 — `phone-tasks/` 下有 6/5 的两条测试记录

**❌ 未实现的功能（手机端查不到）：**
- 手机端(飞书)主动**查询待办任务列表** — 没有暴露查询接口
- **刷新**任务列表 — 没有
- 任务状态**变更**（完成/取消） — `index.json` 里永远是 `task-pending`
- 飞书 Channel 里完全没引用 phone-tasks 相关逻辑

也就是说，**自动记录是好的，但手机端没法看也没法操作**。要做吗？实现这几个功能：
1. 飞书收 `/tasks` 指令 → 回复待办列表
2. `archiveConversation` 用 `editMessage` 更新飞书消息 → 增加"标记完成/刷新"按钮
3. 或者通过 MCP SSE Server 暴露任务查询工具给 Claude

你倾向哪种方式？
