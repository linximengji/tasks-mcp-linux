我的想法是，不改变目前的主流分配规则，而是把allocator这边的流量分配改成多模型。根据不同类型， allocator去tokenplan不同模型，毕竟成本都比DeepSeek高，重点是可用模型数量变多，需要注意的是allocator的分配规则细化。
还有上面提到的tag，可以给一个合适的对应清单。
model proxy的bypass直通模式需要同步配合吗？
还有同时调用多模型，执行任务时，也多了更多选择。
按照这几点要求，调整方案
