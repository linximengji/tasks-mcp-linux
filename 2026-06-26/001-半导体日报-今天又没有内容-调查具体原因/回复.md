根因与刚才调查一致——今早 08:02 DeepSeek V4 Pro 临时输出异常，将所有 RSS 文章标记为"丢弃"，导致：sentinel 已写入、邮件已发出（空内容）、HTML 当时是空的。

不过我 08:28 的 `--dry-run` 已覆盖了 HTML 和 JSON（现在数据文件有 19 条正常内容），加上我刚加的**质量门禁修复**也已到位。但现在 sentinel 存在、HTML 也存在，自动不会重跑了。

需要我执行 `npx tsx src/pipeline.ts --force` 让它带着今天的内容重新发邮件吗？
