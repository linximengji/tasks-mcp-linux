Dashboard 正常工作，所有改动已经生效。Check Status 矩阵现在：

| 格子 | 状态 | 子细节 |
|------|------|--------|
| **Proxy** | `● up` | — |
| **Proxy Backup** | `● up` | — (新增) |
| **Claudetalk** | `● up` | `PID 5056` (独自标识) |
| **MCP Server** | `● up` | `:9877` (新增, 独立进程) |
| SSL / Logs / System / Zombies | 各状态 | — |
| ~~Memory Health~~ | — | 已移除 |

Proxy 卡片里的 backup 行也从死代码 `:4001` 修到了 `:4002`，显示 `12.3 ms`。
