改完了，扁平数组 `cards` 直接驱动渲染，每个 card 字段一致：

- **数据层**: `items` → `cards`，去掉不一致的 `detail`/`note`，统一 `name, pid, port, uptime, status, latency`
- **渲染层**: 表格 → `.grid` 卡片网格，去掉重复的 Ops-Daemon 额外卡片（之前 table 一条 + 下方单独 `.card` 一条）
- **变量名缩短**: `claudetalk` → `clt`

Build 通过，还有问题再调整。
