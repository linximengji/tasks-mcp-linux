TypeScript 编译通过，无错误。

改了三处：
1. **`status.ts`** — 新增 `getPact()`，直接读 `latest.json` 返回 `defense_layers`
2. **`server.ts`** — 注册 `GET /api/pact` 路由，导入 `getPact`
3. **`index.html`** — `renderPact()` 改为 fetch `/api/pact`，不再依赖 `/api/status` 的 `defense_layers` 字段
