改完了。核心变更：

**数据层**：`services: SvcEntry[]` 扁平数组，每项统一 `name/status/detail`，不再有 `╰` 嵌套

**渲染层**：每项一行 `✅ **Proxy**  :4000 /v1/models✅`，尾部追加系统信息和运行时间

```
✅ **Proxy**  :4000 /v1/models✅
✅ **Dashboard**  :8765
✅ **Tunnel**  cloudflared
✅ **ops-daemon**  Proxy主

**系统**  CPU45% 内存60%  C:55% D:70%
ClaudeTalk✅ PID:1234
**运行**  1天2小时3分 (PID:5678)
```

编译通过。需要在飞书里实际 `/status` 看效果，有问题再调。
