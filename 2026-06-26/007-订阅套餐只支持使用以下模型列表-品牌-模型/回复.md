明白，实际可用的模型比我之前查的全。核心变化：`qwen3-coder-plus` 不在套餐内，但 `kimi-k2.7-code` 作为代码专精模型出现了，`glm-5.2` 是最新版。

直接基于真实模型清单出最终方案。

---

## 修订：MultiModelAllocator（基于实际模型列表）

### 实际 TokenPlan 文本模型清单

| 品牌 | 模型 | 协议 | 定位 |
|------|------|------|------|
| 千问 | qwen3.7-max | Anthropic | 最强推理 |
| 千问 | qwen3.7-plus | Anthropic | 性价比+视觉 |
| 千问 | qwen3.6-plus | Anthropic | 视觉中端 |
| 千问 | qwen3.6-flash | Anthropic | 最低成本 |
| Kimi | kimi-k2.7-code | OpenAI | **代码专精** |
| Kimi | kimi-k2.6 | OpenAI | 推理+视觉 |
| GLM | glm-5.2 | OpenAI | 中文创作 |
| GLM | glm-5.1 | OpenAI | 文本生成 |
| MiniMax | MiniMax-M2.5 | OpenAI | 创意文本 |

**不参加自动分配但 @tag 可达：** kimi-k2.5、glm-5、deepseek-v3.2

### Allocator 分配矩阵

`trivial` 和 `simple` 不进 Allocator——保持走 flash/pro。只对 `moderate` 和 `complex` 触发分流。

| complexity | task_type | 首选 | target_cost | min_ratio |
|-----------|-----------|------|-------------|-----------|
| moderate | code | kimi-k2.7-code | ~2x | 0.15 |
| moderate | creative | glm-5.2 | ~2x | 0.12 |
| moderate | reasoning | — | — | — |
| moderate | long_context | qwen3.6-flash | 0.5x | 0.05 |
| moderate | general | — | — | — |
| complex | code | kimi-k2.7-code | ~2x | 0.20 |
| complex | creative | glm-5.2 | ~2x | 0.15 |
| complex | reasoning | qwen3.7-max | 5x | 0.30 |
| complex | long_context | qwen3.7-plus | 1x | 0.15 |
| complex | general | qwen3.7-max | 5x | 0.30 |

**空行 = 留在 DeepSeek，不走 TP。** 整个 moderate+general 和 moderate+reasoning 的主流通路不碰。

### @tag 别名清单

```python
# 按用途
@coder   → kimi-k2.7-code      # 代码专用
@glm     → glm-5.2              # 中文写作
@plus    → qwen3.7-plus         # 性价比+视觉
@cheap   → qwen3.6-flash        # 最低成本
@kimi    → kimi-k2.6            # Kimi推理
@max     → qwen3.7-max          # 最强
@minimax → MiniMax-M2.5         # 创意

# 按供应商
@qwen     → qwen3.7-max
@deepseek → deepseek-v4-pro
@doubao   → doubao-1.5-vision-pro
@ds       → qwen3.7-max-ds       # DashScope按量

# 系统tier（不变）
@flash    → deepseek-v4-flash
@pro      → deepseek-v4-pro
@vision   → doubao-1.5-vision-pro
```

### 降级链

```
kimi-k2.7-code  → deepseek-v4-pro （API 不可用 → DeepSeek 顶替）
glm-5.2         → deepseek-v4-pro
qwen3.7-plus    → deepseek-v4-pro
qwen3.7-max     → qwen3.7-max-ds （TokenPlan 耗尽 → DashScope 按量）
qwen3.6-flash   → deepseek-v4-flash
MiniMax-M2.5    → deepseek-v4-pro
```

### 多模型并行调用（后续 Phase）

干完上述后看使用情况再决定。当前手动多模型可以用并发 `@tag` 无法做到——需要 `X-Multi-Model` header 机制，这个另开 Phase。

---

### 改动文件汇总

| 文件 | 变更 |
|------|------|
| `allocator.py` | 重写为 `MultiModelAllocator`，映射矩阵 + 分层 hash |
| `router.py` | L2 system prompt 加 task_type，`_resolve_classifier` 解析三词 |
| `model_proxy.py` | `_budget_adjust()` 替换为 `_allocator_select()`；`_get_alias()` 扩展 12 条目 |
| `litellm_config.yaml` | 新增 7 个 TP 路由（kimi/glm/minimax/qwen-plus/flash 等） |
| `telemetry.py` | `QWEN_MULTIPLIERS` 补 kimi/glm/minimax 估算倍率 |

不改：`handlers.py`, `fallback.py`, `sanitize.py`, `config.py`, `proxy_backup.py`

---

确认方向的话我直接开始写 allocator.py 和 litellm_config.yaml 的代码。需要先新建 `.plan.md` 走 SDD 吗？
