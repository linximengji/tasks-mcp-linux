因为只改了 **ops-dashboard** 的三处文件：

| 文件 | 改了什么 |
|---|---|
| `ops-dashboard/src/api/status.ts` | `/api/status` 把嵌套的 `proxy.ports.*`/`claudetalk`/`mcp_server` 展平成 flat `processes[]` 数组，移除 `defense_layers` |
| `ops-dashboard/src/server.ts` | 新增 `GET /api/pact` 路由给 Pact 页面用 |
| `ops-dashboard/src/public/index.html` | Dashboard/Uptime 页面适配新 `processes[]` 格式，Pact 页面改请求 `/api/pact` |

手机端飞书 `/status` 命令走的是 **claudetalk** (`index.ts:600-652`)，它直接 `readFileSync` 读 `latest.json` 的原始嵌套结构来拼文字消息，**根本没经过 dashboard 的 API**。所以 dashboard 的 API 格式变化对手机端完全无影响。

要同步更新手机端 `/status` 输出，需要改 claudetalk 的 status handler。要改吗？
