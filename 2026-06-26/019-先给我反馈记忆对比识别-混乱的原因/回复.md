两条相关的记忆都指向 `claudetalk` namespace，但它们的描述都混用了 "dashboard" 这个词：

- `status-command-health-check` 描述：`/status 命令通过并发健康检查 proxy、dashboard、tunnel 和模型端点来报告服务状态`
- `status-detection-mechanisms` 描述：`了解 claudetalk 运行状态的所有检测手段及其可靠性`

**两条记忆都没标注"这是飞书里的 /status 文本卡片"**，只说 "dashboard" — 这个 `dashboard` 在记忆里恰好也是 ops-dashboard 的页面名，撞名了。

加上会话重启后，我刚改完 `D:\ClaudeProjects\ops-dashboard\src\public\index.html` 的 `renderUptime()`，`node dist/server.js` 还在后台跑过，当前工作区环境里 dashboard 是最强信号。两条记忆一读 → "status + dashboard" → 直接映射到 dashboard 的 `/api/status` 页面。

**根因链**: 记忆里没有 `context:` 字段标注"这个 status 是飞书里的命令" → 加上我刚改过 dashboard → "/status没变化" 被解析成了 "dashboard Uptime页面没更新"。

要修的话，给两条记忆加 `context` 标签区分场景，来避免歧义。要加吗？
