改好了。`Claudetalk` 状态格子现在会在状态旁显示 `PID {pid}`：

- `up` → 绿点 + `up` + 灰字 `PID 5056`
- `stopped` → 红点 + `stopped`
- `crashed` → 红点 + `crashed` + 灰字 `crashed`

在 Proxy 那个格子也有参考价值：它本来就是单独 card 展示端口和延迟的，claudetalk 在 matrix 里走简洁模式。要重启 dashboard 看效果吗？
