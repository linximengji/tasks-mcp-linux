已经在之前那轮改好了。关键点：

- **`llm_processor.ts:5`** — 定义 `FALLBACK_MODEL = "qwen3.7-max"`
- **`llm_processor.ts:42,102`** — `callDeepSeek` 和 `llmProcessBatch` 都接受可选 `model` 参数
- **`pipeline.ts:78-80`** — 保留率 < 15% 时 `await llmProcessBatch(entries, FALLBACK_MODEL)`，换 qwen3.7-max 重试
- **`pipeline.ts:86-91`** — 重试仍为 0 → 关键词分类兜底
- **`pipeline.ts:152-164`** — retained === 0 跳过邮件、不写 sentinel

之前 `--dry-run` 已经验证过了（54.8% 保留率正常走，没有触发质量门禁，所以看不到换模型分支在执行）。要模拟一下质量门禁触发的路径看看输出吗？
