好，按清理方案合并后的完整指令表：

## 清理合并后的指令清单

### 会话管理
| 规范命令 | 别名 | 说明 |
|---------|------|------|
| `/new` | 新会话 | 清空本地会话记忆 |
| `/session` | 会话 | 查看当前会话详情 |
| `/session list` | 会话列表 | 列出最近会话 |
| `/session name <标题>` | 会话命名 | 为当前会话命名 |
| `/session new` | — | 创建新会话（**需实现**） |
| `/restart` | 重启 | 重启机器人（仅私聊） |
| `/help` | 帮助 | 显示帮助信息 |

**裁掉：** `/fetch`（无实现，幽灵命令）

### 任务列表
| 规范命令 | 别名 | 说明 |
|---------|------|------|
| `/tasks` | 手机待办 | 查看手机待办列表 |
| `加入手机待办` | — | 将最近对话存档到待办（需确认） |

**裁掉：** `待办`（与 `/tasks` 重复且太短易误触）、`手机任务`（多余别名）

### 后台任务
| 规范命令 | 别名 | 说明 |
|---------|------|------|
| `/task run <描述>` | 后台任务 | 启动后台任务 |
| `/task status` | — | 查看任务状态 |
| `/task cancel <id>` | — | 取消任务 |

（不变）

### 远程终端
| 规范命令 | 别名 | 说明 |
|---------|------|------|
| `开启远程` | 打开远程 | 启动 Tunnel + Dashboard |
| `关闭远程` | — | 停止 Tunnel + Dashboard |

**裁掉：** `启动面板`、`打开面板`、`停止面板`、`关闭面板`

### 服务管理
| 规范命令 | 别名 | 说明 |
|---------|------|------|
| `/status` | 状态 | 服务状态、模型路由 |
| `/log` | 日志 | 最近运行日志 |
| `/daemon restart` | 重启daemon | 重启 ops-daemon |

**裁掉：** `重启守护`

---

### 附件：存档确认词调整
| 角色 | 保留 | 裁掉 |
|------|------|------|
| **确认** | 确认、好的、ok | `对`、`是`、`好`（日常聊天高频） |
| **取消** | 取消、不算 | `不`、`不是`、`不要`、`no`（太宽泛） |

---

你觉得这个预期列表怎么样？有想调整的地方吗？确认后我逐项改代码。
