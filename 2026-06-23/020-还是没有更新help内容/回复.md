已更新 help text，主要改动：

1. **`/session`** 行补全了三个子命令（不加参数、list、name、new）
2. **新增说明**：告知用户历史会话由 Anthropic 服务端维护，CLI 无法删除
3. **`/new` 描述微调**：明确是"清空**本地**会话记忆"

生效不需要重启服务——`/help` 命令是运行时读取这份 `HELP_TEXT` 发送的，下次按需调用即取到新版。
