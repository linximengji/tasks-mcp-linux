从 **~30 个 node 进程降到了 9 个**（2 claudetalk + 1 ops-dashboard + 6 MCP）。现在请你**关闭当前会话重新启动 claude**，看看 `0xC0000142` 是否修复。

已做的工作：
1. **清理了 16 个残留 claudetalk 进程** — 最大头
2. **清理了 19 个 tsx test 僵尸进程** — 昨天跑挂的测试
3. **加了 Defender 排除** — `D:\Tools\node_modules\@anthropic-ai\claude-code`

如果重启后还崩溃，告诉我，我继续排查（可能需要重装 claude-code）。
