这些改动在上一轮已经全部完成，类型检查也通过了：

- `/session new` 已删除，功能合并到 `/new`
- `/phone_tasks` 已改名为 `/tasks`
- 多余的 alias 已精简（STATUS_COMMANDS 9→2）
- 错别字 `加入手机代办` 已修正

代码层面没有剩余工作。需要我重启 claudetalk 让改动生效吗（清除老的 `sessionMapCache` 条目）？
