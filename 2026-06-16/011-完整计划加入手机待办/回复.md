已加入手机待办。任务 `2026-06-16/001-记忆提取重构` 状态 `pending`：

**Phase 1** — 简化提取 hook：SessionEnd 退出 curation，只写 staging 不调 LLM
**Phase 2** — 异步精选：复用 SessionEnd spawn 驱动，无 cron
**Phase 3** — 清理废弃物：仅去掉 `_ensure_audit_record()` 调用，保留兼容
