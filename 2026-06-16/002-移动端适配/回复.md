编译通过。改了什么：

**移动端适配（`@media max-width:600px`）**
- 容器的圆角和阴影去掉，body 背景变白，邮件阅读器里更干净
- 基础字号桌面 14px → 移动端 15px
- item 内边距增大（10px → 12px），触摸目标更大
- 标题字体 15px，条目摘要 14px，source-link 12px — 手机上不眯眼
- 条目链接从 `display:inline` 改成 `display:block + padding`，方便手指点击
- summary 区拉满宽度，不留下边白边
- h1 字号 20px → 17px，防止标题换行太挤

**顺便清理**
- 移除 emoji 图标（无实质意义的内容）和 `hover` 伪类（手机没 hover）

用 `npm start` 跑一次就能看到效果。要我现在跑个 `--dry-run` 验证输出？
