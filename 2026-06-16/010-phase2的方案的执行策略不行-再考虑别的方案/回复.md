更新总结：

**Phase 2 策略改为 SessionEnd spawn 而非定时轮询**
- SessionEnd hook spawn curator 子进程（复用现有 `subprocess.Popen(DETACHED_PROCESS)` 模式）
- 读 staging 目录所有文件，批处理
- 失败不动，下次 SessionEnd 自动重试
- **无 cron、无阈值、无人工延迟**

**Phase 3 补充完整调用链审计**
- 每个函数追踪谁调谁 → 基于调用图做决策，而非猜
- 结论不变：`memory_store()` 中去掉 `_ensure_audit_record()` 这一行，其他全保留
