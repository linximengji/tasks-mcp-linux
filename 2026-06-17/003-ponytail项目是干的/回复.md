在 `D:\ClaudeProjects` 工作区和 `memory-mcp` 记忆中都没有找到名为 **ponytail** 的项目。当前工作区中包含的项目有：

- `claudetalk` — 飞书群聊 AI 助手
- `ops-daemon` — Windows 服务统一守护系统
- `model-proxy` — LLM API 代理与路由
- `semi-report` — 半导体产业情报日报
- 其他 MCP 服务（blender-mcp、drawio-mcp、openscad-mcp 等）

**ponytail** 不在其中。是不是记错了项目名，或者是其他仓库里的项目？
