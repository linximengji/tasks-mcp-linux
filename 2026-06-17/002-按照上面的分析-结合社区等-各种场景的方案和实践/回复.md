方案已更新到 `steady-herding-whisper.md`。v2 → v3 的主要变化：

**新增内容**：
- **风险矩阵** — 量化 10 个风险点的严重度/触发条件
- **Phase 5B 配置化阈值** — 将硬编码参数（并发数/超时/重试/token 阈值）改为 `.claudetalk.json` 配置
- **社区模式命名** — Bulkhead Pattern（1C）、Circuit Breaker（4A）、Write-Ahead（1A）、Graceful Degradation（3A/4A）、Exponential Backoff + Jitter（1D）
- **并发上限推导** — 基于 16GB 开发机的量化计算
- **完整依赖图** — Phase 间清晰的箭头依赖关系
- **实现顺序表** — 14 项按 Impact/Effort 排序，Top 4 仅 3.5 天消除 80% 问题
- **不重试操作清单** — `deleteMessage()` 明确标注不重试
- **熔断器 HALF_OPEN 语义** — 3 个实例各自的 threshold/openDuration
- **metrics P95** — 增加 p95ResponseTime 计算

**与 v2 一致的高层框架**：5 个 Phase 的划分和主要项目（原子写、并发控制、退避重试、流式重构、语音降级、熔断器）不变。

需要开始执行吗？建议从 Phase 1 的 1C（全局并发）和 1A（原子写）开始 — 最高 ROI、最低风险。
