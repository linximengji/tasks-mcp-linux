完成了两件事：

**Bug 修复 — death_sentinel 键名**
- `death_sentinel.py:36` 读 `"watchdog"` → 改为 `"meta_watchdog"`，匹配 daemon 写入的 key
- 每 10 分钟死亡 sentinel 触发不必要的 watchdog 重启，今天已产生 ~110+ 条误报 death_note

**日运行报告推送优化**
改动链路：`config.yaml` → `main.py` → `daily_report.py` → `notify.py`
- `notify()` 新增可选 `receive_id` 参数，可以指定发到飞书个人（默认）或群聊
- `daily_report` 任务从 `config.yaml` 读取 `tasks.daily_report.receive_id`
- 配置示例（已添加注释）：
  ```yaml
  daily_report:
    schedule: "0 8 * * *"
    enabled: true
    receive_id: "oc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # 群 chat_id
  ```
  发到群的话需要把 `receive_id_type=open_id` 改成 `chat_id`，但先加参数层支持，类型切换再说更快
