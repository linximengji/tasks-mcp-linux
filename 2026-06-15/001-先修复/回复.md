两处修改总结：

1. **`session_knowledge.py:490-491`** — retry 耗尽（`>= 3`）丢弃项不再计入 `fail_count`，静默跳过。避免过期重试项反复触发 "解析失败" 警告。

2. **`session_knowledge.py:644`** — `+ fail_count` → `+ 1`，计数器只记录"连续失败轮次"，而不是"失败条目数累积"。一次 consume 运行里就算 10 条全挂也只+1。

3. **status 已清零** — 当前 `session_hook_status.json` 的 `consecutive_parse_fails: 0`，下次 SessionStart 不会再发警告。

下次有真实失败时，计数器会从 0 开始增长，而且不会把过期重试计数在内。
