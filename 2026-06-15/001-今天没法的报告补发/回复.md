报告已补发到飞书。无报错输出，说明推送成功。

报修汇总：
- **death_sentinel.py** — 键名已改 `"watchdog"` → `"meta_watchdog"`，明天就不会再看到每 10 分钟的误报 death_note 了
- **daily_report.py** — 读取 encoding 加了 `errors="replace"`，避免 GBK 乱码行导致解析崩溃
- **notify.py** — 支持透传 `receive_id`，如果想发到飞书群聊，在 `config.yaml` 配 `daily_report.receive_id` 为群的 `oc_xxx` 即可
