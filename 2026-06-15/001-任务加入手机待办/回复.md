已完成。

在 `D:\ClaudeProjects\phone-tasks\index.json` 中添加了一条待办：

```
2026-06-15/002-本地git备份方案实现  →  status: pending
摘要: 实现本地git备份方案：写 github-push.sh + ops-daemon task 配置，后台定期推 GitHub
```

手机端查待办用：终端跑 `cat D:/ClaudeProjects/phone-tasks/index.json | grep pending` 或者通过飞书查询。
