对。今天的两个 bug 都是运行时问题——键名不一致导致误判、scheduler 漏注册。这本来就不该靠架构记忆来防御。

架构记忆的定位很清晰：**改代码前读一遍，知道动一个切面会波及哪些地方**。今天改 `death_sentinel.py` 时我确实读了架构段，知道 `protected_pids.json` 是中枢文件——只是 bug 发生在具体 key 名层面，不在架构层。这正好说明分层是合理的，不该把 key 名这种实现细节塞进架构记忆。
