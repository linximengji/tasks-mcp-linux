不对，我做错了。回到问题本身。

你说的是**每次 Resume session 都触发消费**，问题不是"过滤什么内容"，而是**消费模式本身**——每个 SessionEnd 立即 spawn consume，导致每个微短对话都走一次 LLM 调用。

真正的方案应该是：**移除 SessionEnd 的即时消费，全部交给 SessionStart 批量处理**。

现在 `session_knowledge.py:775` 在 SessionEnd 后立刻 spawn consume。改掉它——只入队，不消费。等到下次 SessionStart（`session_start.py:86-104` 已经有 `_consume_backlog()`），把积压的所有 item 一次性 batch 处理。这样：

- 手机端的 N 次 Resume session → 只产生 N 条 queue item
- 等真正有一次 Fresh session（或者你回到电脑上一打开 CC）→ 一次批量 consume 全部处理，一个 LLM 调用搞定
- 你每次对话看到的"1次失败"也不会出现了，因为 consume 根本不立即触发

代价：Resume session 的记忆提取有延迟（直到下次 non-resume session 启动才处理），但 Resume session 几乎不可能产生需要即时回查的关键记忆。

要改吗？
