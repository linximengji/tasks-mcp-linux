语法正确。改好了，没有重启 daemon。

**改动**：删除了 main.py 第 7-22 行的 OpenTelemetry/Jaeger 集成（`BatchSpanProcessor`、`OTLPSpanExporter`、`HTTPXClientInstrumentor`）。

**效果**：
- 不再依赖 Jaeger（port 4317），daemon 启动不再被 OTLP 阻塞
- 不再有海量 `Transient error StatusCode.UNAVAILABLE` 刷日志
- daemon heartbeat 不再因 OTLP 后台线程卡住而超时
- 下次 daemon 被正常重启（或你手动重启）后生效

下次 daemon 自然重启（被 Task Scheduler 触发或你手动 restart）后这个改动就会生效。需要我手动重启 daemon 让它立即生效吗？
