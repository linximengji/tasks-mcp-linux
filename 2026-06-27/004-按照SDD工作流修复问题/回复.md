全部完成。改动汇总：

**`ops-daemon/ops_daemon/tunnel_manager.py`**
- `_kill_port()`: 从 bare try/except/pass 改为带 logging（杀进程记录 PID、返回值、异常）
- `stop()`: 先杀 cloudflared（taskkill），2s 后停 services（之前顺序相反）；taskkill 的 stdout/stderr 都写日志
- CLI 入口加 `logging.basicConfig` 到 stderr，不影响 stdout 的 JSON 输出

**`claudetalk/src/index.ts`**
- `waitForTunnelGone` 超时 15s → 25s
- exec callback 记录 stdout/stderr 以便事后排查
- exec 加 `timeout: 30000` 防止 PS1 卡死永久阻塞

需要现在重启服务生效吗？
