语法检查通过。改动总结：

1. **新建** `ops-daemon/data/working/tool_audit_rejected.json` — dotenv-manager / docker-assist / git-conventional 三条拒绝记录
2. **改 `tool_audit_full.py`** — `build_prompt()` 在 prompt 末尾追加"以下项目已评估并拒绝，请勿推荐"章节，让 LLM 自然避开
3. **改 `tool_audit.py`** — `_build_recs_table()` 加后置过滤，即使 LLM 不听话也直接去重

新增拒绝条目只需编辑 JSON 追加记录，下次全量审计自动生效。
