需要触发审批流程。风险判定逻辑很简单：

- **L3**：消息包含 `rm -rf`、`chmod 777`、`kill -9`、`dd if=`、`mkfs` 等高风险关键词
- **L2**：消息涉及 `.env`、`settings.json`、`credentials` 等敏感路径

直接从手机飞书给 bot 发一条消息，比如 `chmod 777 test.sh` 或 `删除 .env 文件`，就会触发审批卡片。收到卡片后你点批准，我这里实时看日志。
