明白，两个场景都是"我想审核时主动触发，逐条处理"，不需要后台自动弹。

最小实现路径：**两个 Claude Code skill**，用户输入 `/tool-review` 或 `/memory-review` 触发，Claude 读现有数据源，在对话中逐条展示+收集决策，写回对应状态文件。

- **tool-review skill**：读 `tool_audit_cache.json` 的 LLM 推荐 → 逐条展示 → accept(加入 rejected.json)/install(执行 MCP/skill 安装)/keep(跳过)
- **memory-review skill**：读 `review-report.md` 的 L3 标记项 → 逐条展示 → accept(保留)/reject(删除)/edit(修改内容)

不需要新后端、不需要 Web UI，纯 skill + 已有数据文件。要我出个实现方案吗？
