上一轮方案中还有几个选择没敲定，需要你确认：

1. **Claudetalk 命令改名**：`/phone_tasks` → `/tasks`，可以吗？
2. **tool-review 交互入口**：做成 Claude Code skill（`/tool-review` 终端逐条审核），还是集成到 `cli.py` 命令行模式？
3. **memory-review skill**：review-report.md 目前空的，等有内容再建？
4. **实施范围**：先做 1-3 步（数据模型 + CLI 增强 + Claudetalk 适配），还是 4 步全做？
