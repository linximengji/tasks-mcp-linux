清理工作本身已完成——两处 `modulePath=${import.meta.url}` 已删除。

但编译检查时发现两个已有 TS2802 错误（第 44、103 行，`[...pendingApprovals.keys()]` 和 `[...pendingApprovals.entries()]` 的 MapIterator spread），这是我改动之前就存在的。需要一并修掉吗？只需把 spread 改成 `Array.from()` 即可。
