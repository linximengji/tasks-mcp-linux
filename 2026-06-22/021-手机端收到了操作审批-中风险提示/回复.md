已在前两轮完成调查，重复一下结论：

**根因：** `handleApprovalCallback` 收到回调时，`pendingApprovals` Map 中找不到对应的 `request_id`（`apr_mqp4nb4l_1`），返回了 "审批请求已过期或不存在"，Promise 未 resolve，180 秒后超时触发 "操作已取消"。

日志时间线确认回调 32 秒内到达，不是超时问题。所有回调字段（`action_type`、`request_id`、`decision`、`clicker_id`）完整正确。

**最可能的根因：** 存在**两个 claudetalk 进程**同时连接飞书 WebSocket。A 进程注册了审批 → 发卡片；B 进程收到了卡片回调 → 但 B 的 `pendingApprovals` 是空的。stderr 日志多次出现 `Port 9877 already in use, MCP server not started (another claudetalk instance?)` 印证了多实例的可能性。

需要我排查多实例问题吗？
