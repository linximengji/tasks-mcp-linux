已发送飞书卡片待办：**重启 ops-daemon**。你手动重启后点"已完成"标记。

修复内容概要（三点）：
1. `ops_daemon/main.py:438` — `"npx"` → `"npx.cmd"`（Python 3.13 Windows 找不到无 `.exe` 的命令）
2. 创建 `daily_report/sections/tool_audit_full.py`（ModuleNotFoundError）
3. 两份报告回调里补回 sentinel 写入（重构时丢失）

现在 daemon 用的是旧代码，今天早上的两份报表已手动补发完成。
