收到，拒绝回调走通了。`handleApprovalCallback` 里 `decision='reject'` → `finalizeApproval(requestId, false, 'callback')` → `req.resolve(false)` → CC 侧收到拒绝信号后终止操作。

飞书卡片上应该看到了 `❌ 已拒绝` 的 toast。如果你没有看到 toast 或回调没生效，告诉我日志里的线索，我来查。
