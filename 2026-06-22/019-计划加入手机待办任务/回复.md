已加入手机待办。任务 `2026-06-22/003-phone-tasks独立为待办管理器`：

方案 B（MCP 服务独立），分两步：
- **Step 1**：`phone-tasks/` → `tasks/`，schema 扩展加 `type/source/actions`，`cli.py` 增强，ops-daemon 通过 CLI 推任务
- **Step 2**：建 `tasks-mcp.ts` MCP Server，Claudetalk 改为 MCP 调用，CC skill 终端审核入口

回终端后执行 `python cli.py` 可见列表。
