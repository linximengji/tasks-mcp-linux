旁路测试完成。完整结果：

| 测试 | 状态 | 说明 |
|------|------|------|
| **单元测试** | ✅ | strip_thinking_blocks 4 case / tool_call ordering 4 case |
| **/health** | ✅ 200 | `{"status":"ok"}` |
| **/v1/models** | ✅ 7 models | 全部路由加载正常 |
| **/v1/stats** | ✅ 运行中 | uptime/stats 正常 |
| **/v1/rules/debug** | ✅ L1 路由正确 | trivial→flash, image→qwen3.6-plus, very-long→qwen3.7-max |
| **/v1/messages (stream)** | ✅ SSE 流式 | thinking 直通正常 |
| **E2E deepseek-v4-pro** | ✅ stop_reason | 非流式正常 |
| **E2E qwen3.7-max** | ✅ type=message | 非流式正常 |
| **E2E qwen3.6-plus** | ✅ type=message | 非流式正常 |
| **Sanitize: system** | ✅ | system→top-level |
| **Sanitize: thinking** | ✅ | thinking blocks clean pass |

生产文件已包含所有改动，不需要额外替换步骤。下次 proxy 重启后生效（当前正在运行的 proxy 还在旧代码，不影响——它是独立进程）。你可以随时用 `python D:\ClaudeProjects\proxy_restart.py` 触发重启使改动生效。
