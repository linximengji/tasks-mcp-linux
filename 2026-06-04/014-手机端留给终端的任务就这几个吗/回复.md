不止。回溯整个对话，手机端的完整待办清单：

## 待办（终端处理）

### 权限架构
1. Claudetalk `--dangerously-skip-permissions` → 改为内部权限策略引擎
2. 清理幽灵 hook（4 个未注册文件）
3. 清理 `node_modules/resolve/` 残留 settings
4. 统一跨项目 deny 列表

### 手机端功能
5. **手机/PC 识别** — 你说"调查一下再下结论"，还没做调查。但既然输出截断被你否了，识别终端类型的理由就少了一个——还有别的理由要用吗？（比如限制长任务的 token 预算）
6. **飞书快捷命令集** — `/token`、`/session`、`/approve`、`/task`、`/diff`
7. **飞书审批流** — 高风险命令发飞书卡片等批准（依赖 #1 权限引擎先到位）
8. **任务生命周期管理** — `/task` 触发后台执行 + 进度上报 + 完成通知
9. **会话切换** — `/session list/switch/rename/new`
10. **Code review 紧凑模式** — 手机端只发问题摘要，不发完整 diff

### 已否决（不做）
- ~~输出截断到 800 字符~~
- ~~会话自动归档 + AI 摘要~~
- ~~Git worktree 管理~~
- ~~权限 5 级分级~~

### 未决
- #5 手机/PC 识别：除了格式调整，还有没有别的用处需要这个信号？
