没有类型错误了。4 个 provider 全部查询成功，`daily_spend` 为 null 是因为当前没有前一天（5/31）的快照做对比，这不影响余额显示。

问题总结：

| 层级 | 问题 | 状态 |
|------|------|------|
| `token_stats.py:480` | `date.today()` 返回 date 对象，直接传入 `query_all_with_spend()` | 已加 `_norm_date` 防御 |
| `balance_query.py:69` | `e["date"]` (str) 和 `td` (date) 做 `<` 比较 | 已有 `_norm_date()` 统一转 str |
| 余额输出 | 4 provider 全正常返回 | 已验证 |
