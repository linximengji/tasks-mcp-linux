TypeScript 编译通过，分类逻辑验证合理：

```
台积电 3nm 良率突破              → 制造工艺 ✓
ASML 新一代 EUV 光刻机交付       → 设备材料 ✓
SK海力士 HBM4 CoWoS 量产        → 先进封装 ✓
美国对华出口管制 AI芯片           → 市场供应链 ✓
大基金三期 产业政策              → 产业政策 ✓
碳化硅衬底 天科合达 市场份额      → 设备材料 ✓（"碳化硅"先于"市场"命中）
英伟达 AI加速器 RISC-V          → 芯片产品 ✓
中芯国际 14nm 产能扩产           → 制造工艺 ✓（"14nm"先于"产能"命中）
```

改动汇总：

| 文件 | 改动 |
|------|------|
| `config.ts` | CLASSIFICATION_KEYWORDS 拆为 6 类，CAT_STYLE/CAT_ORDER 同步 |
| `excel.ts` | 硬编码数组 4→6 |
| `send.ts` | 硬编码数组 + emoji 映射 4→6 |
| `pipeline.ts` | 硬编码数组 4→6 |

`classify.ts` 和 `render.ts` 未改——它们通过 `Object.keys()` / `CAT_ORDER` 动态引用，自动跟着 config 走了。
