已手动补发。

不过目前没有自动调度——没有 cron、没有 Windows 定时任务、没有 hook。需要我设一个每日自动发的机制吗？有几种方式：

1. **Windows 任务计划程序** — 每天固定时间跑 `python token_stats.py --send`
2. **Claude Code hook** — 在 session_start 时检查当天是否已发，没发就补
3. **watchdog** — 在现有 proxy watchdog 里加一个每日定时器
